# Локальные настройки Claude Code (могут содержать приватное)
.claude/settings.local.json

# Системное
Thumbs.db
desktop.ini
.DS_Store
*.log

# ⚠️ my-brain/ едет в репо ШАБЛОНОМ (чтобы видна структура). После знакомства он
# наполнится твоими личными ответами. Если будешь выкладывать форк ПУБЛИЧНО —
# раскомментируй строку ниже, чтобы личное не попало в сеть:
# my-brain/
